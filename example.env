# Frontend Configuration
# ----------------------
# [Option 1: Production / Cloudflare Tunnel / Docker]
# Leave VITE_API_URL empty. This enables relative paths (e.g., /api/login).
# Nginx (in the frontend container) will reverse-proxy these requests to the backend.
# This avoids CORS issues and works perfectly with Cloudflare Tunnels (e.g., https://tool.xmlangel.uk).
VITE_API_URL=

# [Option 2: Local Development (npm run dev)]
# If you are running the frontend locally without Docker (e.g., npm run dev),
# create a file named 'frontend/.env.local' and add:
# VITE_API_URL=http://localhost:8000
# This points the local frontend to the local backend.


# Database Configuration
# ----------------------
# This URL is for running backend scripts LOCALLY (outside Docker).
# Docker Compose services use the internal hostname 'postgres' (see docker-compose.yml).
DATABASE_URL=postgresql://user:password@localhost:5436/youtube_stt_db


# Minio Configuration
# -------------------
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
# Internal hostname for Docker network
MINIO_ENDPOINT=minio:9000
# External endpoint for browser access (e.g., presigned URLs)
MINIO_EXTERNAL_ENDPOINT=localhost:9002
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=youtube-stt
MINIO_SECURE=False


# Default Admin Account
# ---------------------
# These credentials are used to create the default admin account on first startup.
# Change these values BEFORE running docker-compose up for the first time.
# To change after initial setup, you need to reset the database or manually update the user.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123


# LLM / OpenWebUI Configuration
# -----------------------------
OPENWEBUI_URL=http://localhost:3000
OPENWEBUI_API_KEY=your_api_key_here
OPENWEBUI_MODEL=llama3