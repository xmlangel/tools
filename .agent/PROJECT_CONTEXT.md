# Project Context & Preferences

## Environment
- **Docker**: The project is built and run in a Docker environment using Docker Compose.
- **Build**: Use `docker-compose build` and `docker-compose up` for running the application.

## Language
- **Korean**: All responses and commit messages should be in Korean.

## Notes
- Frontend is located in `frontend/`
- Backend is located in `backend/`
